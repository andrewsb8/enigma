package awturn

import (
	"enigma/src/awgame"
)

func BuildUnits(game *awgame.Game, player_index int) {
	// for game.Players[player_index], loop through all captured properties
	// and place units on the bases if there is enough funds
	// eventually, might want to find all bases and locations and
	// use them and the map state to determine where to buy what
	return
}
